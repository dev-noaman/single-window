{
  "name": "portal-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:fetch": "playwright test fetch-codes"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
